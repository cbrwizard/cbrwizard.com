@import './../../support/variables/structure.import';

// Effect in a footer

.effect {
  &, .effect__three-js{
    position: relative;
    height: $footer-height;
  }

  canvas, img {
    position: absolute;
    top: 0;
    left: 0;
    height: $footer-height;
    width: 100%;
  }
}
